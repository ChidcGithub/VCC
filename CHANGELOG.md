# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [Unreleased]

### 对话窗口大改（对标 chat.deepseek.com）

- **全新布局**：左侧会话侧栏 + 居中消息流 + 底部胶囊输入条。布局规格实测自官方页面 CSS（侧栏 261px / 消息流 max 840px / 输入条圆角 24px / 会话项高 40px·圆角 12px / 新对话胶囊按钮圆角 100px），保留 VCC 的呼出弹入、工具执行行、悬浮识别字幕等自有组件
- **多会话存储**：sessions.json 按 DeepSeek 会话列表模型重构——会话按「今天 / 昨天 / 7 天内 / 30 天内 / 更早」自动分组，未命名会话以首条用户消息截 24 字自动作标题；支持切换 / 行内重命名（⋯ 菜单）/ 删除（删当前会话自动落到最近会话）/ 一键清空全部；旧 history.json 首次启动自动迁移为一个「历史对话」会话。切换与删除在 Agent 执行中会被拒绝（防写回竞态）
- **深浅双主题**：`body.dark` / `body.light` class 切换（与 DeepSeek 同机制），token 双套校准官方观感（品牌蓝 #4D6BFE、深底 #292A2D、用户气泡蓝灰）；侧栏底部一键切换 + 设置面板可选，偏好持久化
- **消息流重排**：用户消息右对齐蓝灰气泡（尾角收窄），AI 回复全宽纯文本排版；输入条升级为自适应增高 textarea（Enter 发送 / Shift+Enter 换行），↑↓ 翻阅输入历史仅单行时接管方向键
- **窗口升级**：默认 1020×720（原 412×672），最小 620×520；侧栏可折叠（滑出屏左，窄窗口默认收起，状态记忆）
- 语音、跑马灯状态机、TTS、设置面板、拖文件打开等能力不受影响

### 状态机重构

- **跑马灯仅在语音录音与工具执行时显示**：呼出待命（没开麦）、LLM 思考/流式输出、纯文字问答不再全屏亮灯；`listening` 语义改为「麦克风真录音中」，呼出窗口拆分为新状态 `summoned`（主窗「聆听中…」提示保留）
- **修复：延迟隐藏竞态**——idle 排定的 950ms 延迟 hide 会误杀 950ms 内新开始的录音跑马灯；现以代际计数器使过期的 hide 失效
- **完成绽放统一**：工具流程收尾不再直接跳 idle（跳过绽放），改为与文本流程一致发 `done`；done→idle 的 900ms 回落统一收敛到 setPhase 一处
- **执行中呼出不覆盖状态**：Agent 执行中按热键呼出主窗，phase 不再被重置为待命态
- **录音收尾修复**：误触（<400ms）/无音频/识别失败时恢复录音前状态（原先部分路径无收尾）；重复按下麦克风不再泄漏音频流

## [0.5.0] - 2026-09-04

### 功能

- **屏幕文字 OCR（ocr_screen，第 21 个工具）**：Windows.Media.Ocr（系统自带、免费、中文语言包优先）识别屏幕/指定窗口里的文字——UIA 读不到的图片、PPT 缩略图、自绘界面的兜底方案；截图与识别在运行时经 Windows Runtime 完成，不静态链接 api-ms-win-core-winrt（部分受限环境的 loader 解析不了它，进程 0xC0000139）
- **AI 自定义弹窗（show_dialog，第 22 个工具）**：模型自定义标题/正文/按钮（normal/primary/danger 三种样式，1-4 个）；Agent 阻塞等待用户点选并拿到结果，超时/Esc/Alt+F4 均优雅收尾；需要确认、授权、二选一时不再自问自答。实现走 **Win32 原生 TaskDialog**（comctl32 v6 运行时动态解析，零静态 import）：弹窗贴合主窗上方、默认焦点=第一个 primary、danger 按钮触发系统警告图标、内置超时计时器自动收起；原 WebView 玻璃弹窗（ui/dialog.html）移除
- **剪贴板读写（clipboard，第 23 个工具）**：get/set 文本；「帮我复制这段」「念一下我刚复制的内容」闭环
- **测试基建迁移**：全部测试统一挂 bin target（`[lib] test = false`）——lib 测试 exe 的部分系统 DLL import 在受限环境 loader 解析失败（0xC0000139），bin 链接路径不受影响；测试 11/11（含真实 whisper 推理、UIA、OCR 全屏识别、剪贴板 roundtrip）
- **屏幕视觉（read_screen，第 20 个工具）**：DeepSeek 看不了截图，改用 Windows UI Automation 把前台/指定窗口的可点击元素（按钮/菜单/输入框）连同中心坐标 dump 成结构化文本——配合已有 mouse_click/type_text/press_hotkey，模型首次真正"看得见、点得动"任意应用界面；前台是 VCC 自己时自动退回可见窗口列表
- **修复：中文/空格路径下语音识别必然失败**——whisper 二进制按 UTF-8 解释 argv，仓库路径含「高二」等中文时模型加载直接 fail-fast（0xC0000409），报错只剩一行 `load_backend`。现改为 cwd=models + 纯 ASCII 相对文件名传入（server 与 CLI 同修）；wav 临时路径含非 ASCII 时自动落相对名兜底
- **whisper 故障可诊断**：server stderr 落盘 `whisper-server.log`（原为静默丢弃）；server 启动即退出时秒级报错并附日志尾部（原傻等 30s）；CLI 失败信息带退出码 + 日志尾部
- **中文引导 prompt 改走 HTTP 表单**：argv 传中文会被 CRT 转 GBK 再按 UTF-8 解释成乱码，识别引导改为请求体内 UTF-8 注入
- **音量相对调节**（第 19 个工具）：「大点声/小点声」一轮调用搞定，不再需要查音量→设音量两轮往返；静音状态自动解除
- **全局错误兜底**：前端未捕获异常进对话流提示，不静默白屏
- **Ctrl+N 新对话**：键盘流快捷键（Esc 收起、↑↓ 翻历史之外补充）
- **发布包瘦身**：release 开启 strip + thin LTO + 单 codegen unit，体积显著减小
- **工具执行耗时显示**：每个工具行完成后右侧标注真实耗时（ms/s），性能可视化
- **错误一键重试**：API 类失败气泡带「重试」按钮重发上一条指令；语音识别失败给「按住麦克风再说一次」引导
- **空状态快捷指令**：无历史时展示欢迎卡与常用指令胶囊（打开记事本/音量 50/计算器/静音），点击即发
- **全屏截图工具**（第 18 个工具）：「截个图」→ 虚拟屏全捕（多显示器）→ 保存并打开
- **识别完成提示音**：短正弦叮声，教师背对学生时靠听觉确认识别成功
- **录音 60s 上限**：超长录音自动停止，防低配设备推理卡顿
- **Key 错误直达设置**：401/403 错误气泡按钮变「打开设置」，一键直达
- **server 空闲回收**：30 分钟无识别请求自动关闭常驻 whisper-server（低配教室机释放约 500MB 内存），下次语音自动重新拉起
- **UI 历史截断**：对话流只渲染最近 40 条，长对话不卡 DOM
- **工具结果截断**：命令/文件类长输出只留前 1500 字进上下文，防请求膨胀拖慢响应
- **悬浮窗高度上限**：步骤过多时窗口最高 420 逻辑 px，不再撑出屏幕
- **文件拖拽**：课件/文件直接拖进主窗即打开，免打路径
- **识别语言选项**：中文 / English / 自动检测（英语课堂场景），切换后自动重启识别服务
- **识别服务状态行**：设置面板实时显示 whisper 环境与预热状态（排障）
- **新对话动效**：旧对话下沉淡出 → 空状态快捷指令回归；执行中禁止误触清空
- **修复：自定义热键从未生效**——「全局热键」设置此前是摆设（硬编码注册），现真正读取配置；键被占用自动回退默认键，不再崩溃
- **热键即时生效**：保存设置后自动解除旧键并注册新键，无需重启
- **AI 回答轻量 markdown**：`代码` 与 **粗体** 内联渲染（HTML 转义先行，防注入）
- **自定义快捷指令**：设置面板一行一条（最多 8 条），空态胶囊即时刷新——教师可放「打开投影仪」等课堂专属指令
- **工具行 label 截断**：超长路径省略号收尾不撑爆气泡
- **未听清提示**：识别纯噪音时明确提示「没听清」而非无声消失
- **SSE 流式合并**：33ms 批量推送 delta（原逐字 IPC 打爆事件管线），长回答更顺滑
- **单实例保护**：二次启动自动呼出已有实例，杜绝双开抢热键/识别端口
- **朗读 AI 回答**（投影课堂）：WebView2 原生 TTS 零依赖，设置开关即时生效；新指令自动打断在读内容
- **whisper 线程自适应**：未手动配置时按逻辑核一半自动选线程（clamp 2-4），低配设备不再过订阅
- **Agent 并发保护**：前端 + Rust 端双重拦截并发轮次（busy 原子锁），杜绝历史锁竞争

### 性能

- **whisper-server 启动预热**：应用启动 20s 后台静默拉起常驻识别服务，首次语音零冷启动
- **工具 schema 精简**：17 个工具描述瘦身，降低每轮请求的 system token 与首 token 延迟

### UI

- 设置面板分组化（连接 / 通用 / 语音识别 / 记忆），hairline 分隔层级更清晰
- 滚动条静止近隐形、hover 显现；空状态呼吸 logo 与胶囊样式
- 消息 hover 显示时间戳；智能滚动（回看历史不被强拉到底，错误气泡始终滚入视野）
- demo 模式同步展示耗时样式

## [0.4.0] - 2026-09-04

### 新增（语音识别效率深度优化——目标：学校低配设备流畅运行）
- **常驻 whisper-server**：模型只加载一次。旧链路每次识别都要冷启动 CLI 进程
  并重新加载 487MB 模型（低配机上 2-8 秒纯等待）；现在首次识别后 server 常驻
  127.0.0.1 随机高位端口，后续识别只花纯推理时间（5s 音频 CPU 通常 1-3s）
- **q5_1 量化模型**（`ggml-small-q5_1.bin`，487MB → 180MB）：加载快约 2.7 倍，
  推理更快，中文指令场景质量损失可忽略；`whisper-quantize` 本地生成
- **server.json 跨实例复用**：记录 port/pid/model——旧实例遗留的健康 server
  直接复用（防孤儿重复驻留），档位不符才 kill 重启
- **识别参数调优**（CLI 与 server 共用）：固定 `-l zh` 跳过语言自动检测、
  greedy 解码（`-bs 1 -bo 1`，短指令基本无损）、中文引导 prompt、无时间戳
- **前端静音裁剪**：送识别前砍掉首尾静音（25ms 窗 RMS 门限 + 150ms 缓冲），
  短指令录音常见 1-3s 静音，推理时间近乎减半
- **识别模型档位**（设置面板下拉）：「快 · q5 量化（推荐，学校设备）」/
  「准 · fp16 原版」；`voice_threads` 配置项可手动锁线程数（0=whisper 默认）
- **VCC_BENCH=1 基准钩子**：真机测语音链路耗时，3 轮写入 bench.log
  （首轮含 server 冷启动，后两轮为热态真实延迟）
- server 两连失败自动降级 CLI 兜底（保底可用性）
- `tools/tests/` 合成测试音频（3s/5s/10s，速度基准用）

### 备注
- whisper 推理在开发 sandbox 中被安全 hook 无法实测（0xC0000139），
  速度数据待真机 `VCC_BENCH=1` 验证；优化项收益为 whisper.cpp 社区公认值

## [0.3.0] - 2026-09-04

### 新增
- **对话历史持久化**：每轮 agent 结束后写入 `%APPDATA%\com.chidc.vcc\history.json`，
  重启自动恢复对话流（渲染 user/assistant 气泡 + 「上次对话」分隔线，
  工具中间轮不进 UI 但保留在 LLM 上下文里）
- **AI 长期记忆**（跨会话）：
  - 新模块 `memory.rs`：`memory.json` 存记忆摘要，注入 system prompt
    （「用户长期记忆」块，截断 800 字）
  - 自动触发：上下文超 60 条压缩时，被裁掉的旧消息后台并入记忆；
    点「新对话」时当前会话精华先归档入记忆再清空
  - 总结 prompt 只保留长期有价值信息（身份习惯/设备环境/常用指令/任务模式），
    200 字内分条陈述；后台异步执行，失败静默，绝不干扰主对话
  - 设置面板新增记忆区块：可查看/手动编辑/一键清除；
    对话流在记忆更新后显示淡色「记忆已更新」分隔线
- 新增 Tauri 命令：`load_chat_history` / `get_memory` / `save_memory_cmd`
- `llm.rs` 新增 `chat_completion_simple` 非流式接口（记忆总结专用）

### 修复
- **历史恢复时序竞争**：历史恢复原本在 `setup_windows()` 之后执行，而主窗
  webview 页面加载完成即 invoke `load_chat_history`——实测拿到空数组。
  恢复逻辑上移到窗口创建之前（E2E 日志实锤：cmd returning 0 → 4 msgs）
- 历史裁剪逻辑 bug：旧代码把 `hist[0]` 误当 system 消息保留
  （AppState.history 从不含 system），导致第一条消息被永久保留

### 变更
- 主窗口卡片圆角 26px → 14px（更利落）
- 设置面板内容超高时可滚动

## [0.2.15] - 2026-09-04

### 修复
- **卡片后方「淡灰色模糊方框」终极根治**（前三轮只修了表象）：
  1. **直接来源**：v0.1.0 残留的 `window_vibrancy::apply_acrylic`——主窗/悬浮窗
     被涂了一层 Acrylic 毛玻璃（tint `rgba(20,20,24,0.49)`），与黑白极简卡片
     体系冲突。删除
  2. **放大器**：Win11 的 DWM 会对 blur-behind 透明窗口自动套用系统 backdrop
     材质（Mica/Acrylic = 模糊 + 灰调）。三个透明窗口显式设
     `DWMWA_SYSTEMBACKDROP_TYPE = DWMSBT_NONE`。像素级采样：环区与窗外
     diff 从 ~128 降至 ~10（残差为 done 白晕）
  3. **现代化合成路径**：本地 patch tao（`vendor/` + `scripts/patch-tao.py`）——
     透明窗口改用 `WS_EX_NOREDIRECTIONBITMAP`（Chromium/Electron 同款方案）
     替代 legacy blur-behind glass
  4. 保险：`WEBVIEW2_DEFAULT_BACKGROUND_COLOR=00FFFFFF`（官方变量，防加载期底色）

### 变更
- 移除 `window-vibrancy` 依赖（无引用）
- 设置面板显示运行中版本号（v0.2.14，保留）

## [0.2.14] - 2026-09-04

### 新增
- 设置面板显示**运行中的真实版本号**（`getVersion`）——一眼确认
  「安装的包」与「正在跑的进程」是否一致（排查灰框问题时发现用户
  实例可能停留在旧版本）

### 验证
- 真机像素采样诊断（浅色/深色背景）：0.2.13 透明边距环 6/8 测点与
  窗外完全一致（diff=0），窗口环已完全透明——若仍见灰框请先确认版本号

## [0.2.13] - 2026-09-04

### 修复
- **卡片后方淡灰色模糊方框根治**：`main` / `floating` 两个透明窗口未关闭 DWM
  系统阴影——Windows 会沿**窗口矩形**画一圈系统阴影，正好落在卡片外的透明边距上，
  呈现为与卡片圆角不符的灰色方框（overlay 窗口已有 `shadow(false)`，另两个漏了）。
  两窗口补 `.shadow(false)`
- 移除 `.card` 自身的黑色投影（26px 边距装不下 48px 模糊半径，浅色壁纸上晕成灰环）；
  状态白色呼吸光保留

## [0.2.12] - 2026-09-04

### 修复
- **主窗四周深灰环根治**：全窗噪点层（`body::after`）使用 `mix-blend-mode: overlay`，
  混合模式强制给透明区域垫不透明黑底——卡片外的透明边距整圈变深灰（0.2.11 加大边距后
  环随之变宽暴露根因）。移除该层
- 移除 `.card` 的 `backdrop-filter`（透明窗口内无窗口内容可模糊，空操作），卡片背景
  alpha 0.72/0.82 → 0.88/0.94 补偿玻璃深度

## [0.2.11] - 2026-09-04

### 变更
- 卡片边距 10 → 26px、窗口 380×640 → 412×672：阴影（70px/0.55 → 40px/0.38）在余量内
  自然渐隐，不再堆积

## [0.2.10] - 2026-09-04

### 视觉
- **双扫光高光减淡**：沿边缘旅行的主/副两个白色镜面高光峰值各压 ~40%
  （1.0 → 0.60 / 0.55 → 0.32），色带亮度不变，高光经过角落不再过曝

## [0.2.9] - 2026-09-04

### 视觉
- **四角填满**：光环延伸到屏幕物理角尖，完整绕屏一圈
  - SDF 圆角半径 46 → 14：光带拐角紧贴物理角尖（旧圆角把角落推远 ~70px，
    角尖处光带距离 82px vs 边缘 12px，是角落暗的主因）
  - edge fade 改用直角盒 SDF 判定（`sdBox(q, c2, 0)`）——旧的绝对 d 阈值
    在角落（d ≈ 17-82px > 12.5px 阈值）会把角尖光整体裁掉

## [0.2.8] - 2026-09-03

### 视觉
- **不对称边缘光场**：光在屏幕物理边缘最厚最亮，向内渐淡；外半边大面积铺开到屏幕边被截断，
  内半边深延展但渐隐——Apple Intelligence 边缘 glow 的真实形态
  - `gaussAsym(d, center, sigIn, sigOut)`：外侧 sigma 1.45×/2.60×（core/bloom），
    内侧 sigma 0.42×/1.55×（更宽延展但高斯渐隐）
  - bloom 重心从 -1.30 内移到 -0.85 宽度（贴边取向）
  - 旧版 edge fade 把最外 8px 压暗，现已反转：只在物理边缘 1.5px 处收口截断

## [0.2.7] - 2026-09-03

### 安全
- 命令拦截扩充：`Format-Volume` / `Stop-Computer` / `Clear-Disk` / `Initialize-Disk` /
  `Invoke-Expression`（含 `| iex` 远程代码执行形态）/ `reg add` / `Start-Process -Verb RunAs`
- `format` 精确化：仅指向盘符时拦截，`-Format 'yyyy-MM-dd'` 类参数不再误伤
- 新增回归测试（`cargo test`，安全逻辑 3 项 + 真机 2 项）

### 修复
- `401/403` API 错误改为引导检查 Key 设置的友好提示

## [0.2.6] - 2026-09-03

### 新增
- 自定义应用图标：黑玻璃圆角方块 + 白色闪电（`scripts/make-icon.py` 程序化生成，可复现）
- NSIS 显式 `currentUser` 安装模式

## [0.2.5] - 2026-09-03

### 新增
- 开机自启：设置面板开关，写/删 HKCU Run 键（`reg.exe`，零新依赖），保存即生效
- 演示置顶：主窗 `always_on_top` 开关，启动时恢复偏好

### 文档
- `CHANGELOG.md`（本文件）

## [0.2.4] - 2026-09-03

### 新增
- 输入历史：输入框 `↑` / `↓` 翻阅历史指令（相邻去重、草稿暂存、50 条上限）
- README 性能实测数据表（光环活跃期 CPU 均值 0.08%，待机停绘 0.00%）

## [0.2.3] - 2026-09-03

### 新增
- `Esc` 一键收起主窗（设置面板打开时先关面板；agent 后台继续跑不中断）

### 验证
- 悬浮窗「完成 → 5s 淡出 → 隐藏」生命周期真机闭环实锤（E2E 三连拍）

## [0.2.2] - 2026-09-03

### 新增
- 输入条 Apple Intelligence 扫光：AI 工作时七色光弧沿输入框边缘流动（`@property` 角度动画 +
  conic-gradient + mask 描边带；done 阶段加速一圈与光环绽放同步）
- 生成中 Shimmer：「思考中…」白色流光扫过灰字（AIGeneratingText 语义的黑白化）

### 无障碍
- 两项新动效均尊重 `prefers-reduced-motion`（静态降级，shimmer 恢复实色）

## [0.2.1] - 2026-09-03

### 修复
- 悬浮窗自 v0.1.0 起从未显示的两个潜伏 bug：
  1. `f-state` 元素缺 `id`，渲染时抛 `Cannot set properties of null`
  2. 全代码库无 floating 窗口 `show()` 调用（Rust 侧补 `vcc://float` 监听器）

### 变更
- 全屏光环 v2：七色粉彩板（社区公认采样）、双层光带（锐核 + 宽柔晕）、定向环流（`uSpin`
  刚体旋转噪声场）、语音电平响应（AnalyserNode RMS → 事件 → 快攻慢放包络）、done 绽放阶段
- 渲染性能：0.6x 分辨率渲染（线性放大近无损，省 64% GPU）+ 完全淡出后停绘
- 主窗呼入动画（summon 弹入 + 涌入入场）；悬浮窗黑白化统一
- 备用热键 `Ctrl+Alt+K`

## [0.2.0] - 2026-09-03

### 新增
- 全屏 Apple Intelligence 光环 Overlay（WebGL 波浪折射实时渲染），呼出即涌入
- 多显示器：每屏一个光环 overlay
- 主窗视觉重做：黑白极简体系（黑玻璃卡片、白主按钮、克制动效）

## [0.1.0] - 2026-09-02

### 首个可用版本
- 全局热键 `Ctrl+Shift+Space` 呼出 / 隐藏，后台托盘常驻
- 按住说话：本地 Whisper（CPU small 模型）转写 → 自动发送
- DeepSeek function calling Agent 循环（流式 SSE 回答 + 工具轮次）
- 17 个系统控制工具：音量 / 亮度 / 鼠标 / 键盘 / 文件 / 应用 / PowerShell
  （删除 / 格式化 / 关机类命令硬拦截）
- 右上角悬浮步骤小窗（毛玻璃 + 步骤状态），完成后 5 秒淡出
